require "spec_helper"

RSpec.describe Tanuki::Universe do
  it "has a version number" do
    expect(Tanuki::Universe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
